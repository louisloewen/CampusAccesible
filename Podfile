source 'https://github.com/CocoaPods/Specs.git'
target 'CampusAccesible' do
  use_frameworks!
  # All pods removed — project now uses native SwiftUI + MapKit + GameplayKit only.
  # Run `pod deintegrate` then remove the CampusAccesible.xcworkspace and use
  # CampusAccesible.xcodeproj directly.
end
